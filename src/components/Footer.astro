<footer class="footer">
    <div class="footer-banner">
        <h2>WinStar</h2>
    </div>
    <div class="footer-top-line"></div>
    <div class="footer-content">
        <div class="footer-section">
            <h3>Contacto</h3>
            <p>Email: info@winstar.com</p>
        </div>
        
        <div class="footer-section">
            <h3>Síguenos</h3>
            <div class="social-links">
                <a href="https://www.behance.net/samantharellan4" target="_blank" rel="noopener noreferrer">
                    <i class="fa-brands fa-behance"></i> Behance
                </a>
                <a href="https://github.com/sam25549" target="_blank" rel="noopener noreferrer">
                    <i class="fa-brands fa-github"></i> GitHub
                </a>
                <a href="https://www.instagram.com/sam.acruz/" target="_blank" rel="noopener noreferrer">
                    <i class="fa-brands fa-instagram"></i> Instagram
                </a>
            </div>
        </div>
    </div>
    <div class="footer-bottom">
        <p>&copy; 2025 WinStar. Todos los derechos reservados.</p>
    </div>
</footer>

<style>
    .footer {
        background-color: var(--color-background);
        color: var(--color-text);
        padding: 0 2rem 1rem;
        margin-top: 4rem;
        text-align: center;
        position: relative; /* Needed for absolute positioning of the line */
    }

     .footer-top-line {
        width: 100%;
        height: 2px; /* Adjust thickness as needed */
        background-color: var(--color-text); /* White line */
        margin-bottom: 3rem; /* Space between line and content */
    }

    .footer-banner {
        background-color: white;
        padding: 1.5rem 0;
        text-align: center;
    }

    .footer-banner h2 {
        font-family: var(--font-family-header);
        font-size: 3rem; /* Adjust size as needed */
        color: var(--color-pop); /* Use the pop color for c ontrast */
        animation: pulse-color 2s infinite ease-in-out;
    }

    @keyframes pulse-color {
        0% { color: var(--color-primary); }
        50% { color: var(--color-primary-osc); }
        100% { color: var(--color-primary); }
    }

    .footer-content {
        max-width: 1200px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 2rem;
        padding-bottom: 2rem;
        justify-content: center;
    }

    .footer-section {
        text-align: center;
    }

    .footer-section h3 {
        color: var(--color-text);
        margin-bottom: 1rem;
        font-family: var(--font-family-body);
    }

    .footer-section p {
        color: var(--color-text-muted);
        line-height: 1.6;
    }

    .social-links {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        align-items: center;
    }

    .social-links a {
        color: var(--color-text-muted);
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        transition: color 0.3s;
    }

    .social-links a:hover {
        color: var(--color-text);
    }

    .footer-bottom {
        text-align: center;
        margin-top: 1rem; /* Reduced margin */
        padding-top: 1rem;
        border-top: 1px solid var(--color-primary-osc);
         background-color: var(--color-background); /* Match footer background */
    }

    .footer-bottom p {
        color: var(--color-text-muted);
    }
</style> 